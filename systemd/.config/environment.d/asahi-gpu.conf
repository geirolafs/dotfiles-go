# Force Vulkan to use Asahi hardware driver for M1 Max GPU
# This ensures D-Bus activated services (like NautilusPreviewer) can access the GPU
VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/asahi_icd.aarch64.json
