# User window rules (extend/override Omarchy defaults)

# WiFi and Bluetooth TUIs - floating windows via Alacritty with custom classes
windowrule = tag +floating-window, class:(Bluetui|Impala)

# Stremio - bypass compositor entirely for direct rendering
windowrulev2 = immediate, class:(stremio)
