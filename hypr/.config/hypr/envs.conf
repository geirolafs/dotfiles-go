# Extra env variables
# env = MY_GLOBAL_ENV,setting

# Terminal emulator
env = TERMINAL,ghostty

# mise runtime manager paths (using shims for version-agnostic access)
env = PATH,$HOME/.local/share/mise/shims:$PATH

# iMac remote access (for VNC scripts)
env = IMAC_IP,100.74.71.71
env = IMAC_USER,m9
