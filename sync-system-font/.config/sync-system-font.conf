# Font override configuration for sync-system-font
# Uncomment to override the font from Alacritty config

FONT_OVERRIDE="SF Pro Text"
FONT_SIZE_OVERRIDE="11"

# To use Alacritty's font again, comment out or delete the lines above
