#!/bin/bash

# Force Alacritty terminal instead of $TERMINAL
if pgrep -f "class.*Bluetui" >/dev/null; then
  hyprctl dispatch focuswindow "class:Bluetui"
else
  /usr/bin/alacritty --class=Bluetui -e bluetui &
fi
